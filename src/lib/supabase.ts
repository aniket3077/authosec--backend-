/**
 * Legacy Supabase client placeholder.
 * All authentication has migrated to Clerk, so this module intentionally throws
 * to help surface any remaining references during development.
 */

export const supabase = undefined as never;
export const supabaseAdmin = undefined as never;

export default supabaseAdmin;
